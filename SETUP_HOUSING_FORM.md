# دليل إعداد نموذج طلب امتلاك مسكن

## الطريقة السريعة (SQL Script)

### خطوة 1: إنشاء النموذج عبر SQL

1. افتح **Supabase Dashboard**
2. اذهب إلى **SQL Editor**
3. افتح ملف `supabase/create-housing-form.sql`
4. انسخ كل المحتوى والصقه في SQL Editor
5. اضغط **Run** أو F5

سيتم إنشاء النموذج تلقائياً مع جميع الحقول الـ 24.

### خطوة 2: التحقق من النموذج

بعد تشغيل السكريبت، ستظهر رسالة نجاح. يمكنك التحقق من النموذج:

```sql
SELECT 
  f.name,
  f.public_url,
  f.status,
  COUNT(fi.id) as field_count
FROM forms f
LEFT JOIN fields fi ON fi.form_id = f.id
WHERE f.public_url = 'housing-request-form'
GROUP BY f.id, f.name, f.public_url, f.status;
```

### خطوة 3: الوصول إلى النموذج

- **الرابط العام**: `http://localhost:3000/form/housing-request-form` (أو رابط موقعك)
- **في لوحة التحكم**: اذهب إلى `/admin` وستجد النموذج في القائمة

## الطريقة البديلة (من لوحة التحكم)

إذا كنت تفضل استخدام الواجهة:

1. سجل الدخول إلى `/admin`
2. اضغط على زر **"إنشاء نموذج طلب امتلاك مسكن"** (الأزرار الخضراء)
3. اضغط **"إنشاء النموذج"**
4. سيتم إنشاء النموذج تلقائياً

## ميزات النموذج

### الأقسام (24 حقل):

1. **المعلومات الشخصية** (5 حقول)
   - الاسم الكامل (إجباري)
   - رقم الهاتف (إجباري)
   - البريد الإلكتروني (إجباري)
   - الحالة العائلية (إجباري)
   - عدد أفراد الأسرة (إجباري)

2. **معلومات السكن الحالي** (5 حقول)
   - الولاية الحالية (إجباري)
   - المدينة / المعتمدية (إجباري - ديناميكي)
   - نوع السكن الحالي (إجباري)
   - قيمة الكراء الشهري (إجباري)
   - مدة الكراء الحالية (إجباري)

3. **الرغبة في السكن المستقبلي** (4 حقول)
   - الولاية المرغوبة (إجباري)
   - المدينة / المنطقة المرغوبة (إجباري)
   - نوع المسكن المطلوب (إجباري - متعدد)
   - الغرض من السكن (إجباري)

4. **الميزانية والقدرة المالية** (5 حقول)
   - الميزانية القصوى (إجباري)
   - طريقة التمويل (إجباري - متعدد)
   - الدخل الشهري (إجباري)
   - هل يوجد قرض حالي؟ (إجباري)
   - قيمة القسط الشهري (اختياري - يظهر فقط عند "نعم")

5. **الجاهزية والجدية** (3 حقول)
   - متى تنوي اقتناء المسكن؟ (إجباري)
   - هل سبق لك التقدم بطلب تمويل؟ (إجباري)
   - هل ترغب في التواصل مع مستشار؟ (إجباري)

6. **معلومات إضافية** (2 حقول)
   - متطلبات خاصة (اختياري)
   - ملاحظات إضافية (اختياري)

### الميزات التقنية:

✅ **ربط ديناميكي بين الولاية والمدينة**
- عند اختيار ولاية، يتم تحديث قائمة المدن تلقائياً

✅ **حقول مشروطة**
- حقل "قيمة القسط الشهري" يظهر فقط عند الإجابة بنعم

✅ **التحقق من البيانات**
- التحقق من صحة البريد الإلكتروني
- التحقق من صحة رقم الهاتف
- التحقق من الحقول الإجبارية

✅ **تصميم متجاوب**
- يعمل على جميع الأجهزة (موبايل، تابلت، ديسكتوب)

## ربط النموذج بالصفحة الرئيسية

يمكنك إضافة رابط للنموذج في الصفحة الرئيسية (`app/page.tsx`):

```jsx
<a 
  href="/form/housing-request-form"
  className="px-8 py-4 bg-white text-blue-700 rounded-xl hover:bg-blue-50 transition-all duration-200 shadow-xl hover:shadow-2xl font-semibold text-lg"
>
  ابدأ طلبك الآن
</a>
```

## عرض الإرسالات

يمكنك عرض جميع الطلبات من:
- **لوحة التحكم**: `/admin/submissions`
- **خريطة المواقع**: `/admin/map` (للعقارات التي تحتوي على حقول موقع)

## ملاحظات مهمة

1. **البيانات محفوظة بشكل آمن**: جميع البيانات محفوظة في قاعدة البيانات ويمكن فلترتها
2. **الأمان**: النموذج العام متاح للجميع، لكن بيانات الإرسالات محمية
3. **التحديث**: يمكنك تعديل أي حقل من صفحة التعديل (`/admin/forms/[id]`)

## حل المشاكل

إذا واجهت مشاكل:

1. **النموذج غير موجود**: تأكد من تشغيل السكريبت SQL بشكل صحيح
2. **الحقول لا تظهر**: تحقق من أن `status = 'active'`
3. **الأخطاء**: تحقق من سجلات المتصفح (F12) وسجلات Supabase

---

**تم الإنشاء بنجاح! 🎉**

الآن يمكنك البدء في جمع الطلبات من المستأجرين.
